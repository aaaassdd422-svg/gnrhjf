<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>إ.أ. محمد علاء جبار سموم</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/cairo@5/index.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/amiri@5/index.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/playfair-display@5/index.css" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js">
    </script>
    <style>
        /* === RESET & BASE === */
        *,
        *::before,
        *::after {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        html {
            scroll-behavior: smooth;
            overflow-x: hidden;
        }
        body {
            font-family: 'Cairo', sans-serif;
            color: #fff;
            overflow-x: hidden;
            background: #0a0a0a;
            cursor: default;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        #three-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            z-index: -2;
            display: block;
        }
        .bg-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background: linear-gradient(to bottom, rgba(0, 0, 0, 0.5) 0%, rgba(0, 0, 0, 0.2) 40%, rgba(0, 0, 0, 0.2) 60%, rgba(0, 0, 0, 0.6) 100%);
            z-index: -1;
            pointer-events: none;
        }

        /* === LOADING === */
        .loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #0a0a0a;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            z-index: 9999;
            transition: opacity 1s ease, visibility 1s ease;
        }
        .loading-screen.hidden {
            opacity: 0;
            visibility: hidden;
        }
        .loading-ring {
            width: clamp(40px, 8vw, 60px);
            height: clamp(40px, 8vw, 60px);
            border: 3px solid transparent;
            border-top-color: #b48c50;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin-bottom: 20px;
            position: relative;
        }
        .loading-ring::before {
            content: '';
            position: absolute;
            top: 5px;
            left: 5px;
            right: 5px;
            bottom: 5px;
            border: 3px solid transparent;
            border-top-color: #d4af37;
            border-radius: 50%;
            animation: spin 0.6s linear infinite reverse;
        }
        @keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }
        .loading-text {
            font-family: 'Cairo', sans-serif;
            font-size: clamp(12px, 2vw, 14px);
            color: #b48c50;
            letter-spacing: 4px;
        }

        /* === SCROLL INDICATOR === */
        .scroll-indicator {
            position: fixed;
            bottom: clamp(20px, 5vh, 40px);
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 10px;
            z-index: 100;
            animation: fadeInUp 1s ease-out 1s both;
            transition: opacity 0.5s ease;
        }
        .scroll-indicator.hidden {
            opacity: 0;
            pointer-events: none;
        }
        .scroll-text {
            font-family: 'Cairo', sans-serif;
            font-size: clamp(12px, 1.6vw, 14px);
            color: rgba(255, 255, 255, 0.7);
            letter-spacing: 3px;
        }
        .scroll-arrow {
            width: clamp(18px, 3vw, 24px);
            height: clamp(18px, 3vw, 24px);
            border-right: 2px solid rgba(255, 255, 255, 0.7);
            border-bottom: 2px solid rgba(255, 255, 255, 0.7);
            transform: rotate(45deg);
            animation: scrollBounce 2s infinite;
        }
        @keyframes scrollBounce {
            0%,
            100% {
                transform: rotate(45deg) translate(0, 0);
                opacity: 0.7;
            }
            50% {
                transform: rotate(45deg) translate(5px, 5px);
                opacity: 1;
            }
        }

        .scroll-progress {
            position: fixed;
            top: 0;
            left: 0;
            height: 3px;
            background: linear-gradient(90deg, #b48c50, #d4af37, #b48c50);
            z-index: 1000;
            transition: width 0.1s linear;
            width: 0%;
        }

        .mode-indicator {
            position: fixed;
            top: clamp(15px, 3vh, 30px);
            right: clamp(15px, 3vw, 30px);
            display: flex;
            align-items: center;
            gap: 10px;
            z-index: 100;
            font-family: 'Cairo', sans-serif;
            font-size: clamp(10px, 1.4vw, 12px);
            color: rgba(255, 255, 255, 0.6);
            letter-spacing: 2px;
            opacity: 0;
            transition: opacity 0.5s ease;
        }
        .mode-indicator.visible {
            opacity: 1;
        }
        .mode-dot {
            width: clamp(6px, 1vw, 8px);
            height: clamp(6px, 1vw, 8px);
            border-radius: 50%;
            background: #666;
            transition: background 0.5s ease;
            flex-shrink: 0;
        }
        .mode-dot.active {
            background: #b48c50;
            box-shadow: 0 0 10px rgba(180, 140, 80, 0.6);
            animation: statusPulse 2s infinite;
        }
        @keyframes statusPulse {
            0%,
            100% {
                box-shadow: 0 0 5px rgba(180, 140, 80, 0.4);
            }
            50% {
                box-shadow: 0 0 15px rgba(180, 140, 80, 0.8);
            }
        }

        /* === SECTIONS === */
        .section {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: clamp(30px, 6vh, 60px) clamp(16px, 4vw, 40px);
            position: relative;
            width: 100%;
            flex-shrink: 0;
        }
        .hero {
            min-height: 100vh;
        }

        .header {
            position: absolute;
            top: clamp(15px, 3vh, 30px);
            width: 100%;
            text-align: center;
            padding: 0 clamp(10px, 3vw, 20px);
            pointer-events: none;
        }
        .header-line {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: clamp(10px, 2vw, 20px);
            margin-bottom: 10px;
        }
        .header-line::before,
        .header-line::after {
            content: '';
            width: clamp(40px, 10vw, 80px);
            height: 1px;
            background: linear-gradient(90deg, transparent, #b48c50, transparent);
        }
        .header-text {
            font-family: 'Playfair Display', serif;
            font-size: clamp(10px, 1.4vw, 14px);
            letter-spacing: clamp(4px, 1vw, 8px);
            color: #b48c50;
            text-transform: uppercase;
            white-space: nowrap;
        }

        .main-content {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: clamp(20px, 5vh, 40px);
            max-width: min(600px, 90vw);
            width: 100%;
        }

        /* === IMAGE WRAPPER === */
        .image-wrapper {
            position: relative;
            width: min(320px, 70vw, 70vh);
            height: min(320px, 70vw, 70vh);
            border-radius: 50%;
            padding: clamp(4px, 1vw, 6px);
            background: linear-gradient(135deg, #b48c50, #d4af37, #f5d78e, #d4af37, #b48c50);
            background-size: 300% 300%;
            box-shadow: 0 0 30px rgba(180, 140, 80, 0.3), 0 0 60px rgba(180, 140, 80, 0.1), inset 0 0 30px rgba(180, 140, 80, 0.1);
            animation: goldShimmer 4s ease-in-out infinite, fadeInUp 1s ease-out;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            flex-shrink: 0;
        }
        .image-wrapper:hover {
            transform: scale(1.02);
            box-shadow: 0 0 40px rgba(180, 140, 80, 0.5), 0 0 80px rgba(180, 140, 80, 0.2), inset 0 0 40px rgba(180, 140, 80, 0.2);
        }
        .image-inner {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            overflow: hidden;
            position: relative;
        }
        .image-inner img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.8s ease;
            display: block;
        }
        .image-wrapper:hover .image-inner img {
            transform: scale(1.08);
        }
        .image-wrapper::after {
            content: '';
            position: absolute;
            top: -4px;
            left: -4px;
            right: -4px;
            bottom: -4px;
            border-radius: 50%;
            border: 2px solid transparent;
            background: linear-gradient(135deg, #f5d78e, #b48c50, #d4af37, #b48c50, #f5d78e) border-box;
            background-size: 300% 300%;
            -webkit-mask: linear-gradient(#fff 0 0) padding-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
            animation: goldShimmer 4s ease-in-out infinite;
            pointer-events: none;
            opacity: 0.6;
        }
        .image-wrapper::before {
            content: '';
            position: absolute;
            top: clamp(-12px, -3vw, -20px);
            left: clamp(-12px, -3vw, -20px);
            right: clamp(-12px, -3vw, -20px);
            bottom: clamp(-12px, -3vw, -20px);
            border-radius: 50%;
            background: radial-gradient(circle, rgba(180, 140, 80, 0.15), transparent 70%);
            z-index: -1;
            animation: glowPulse 3s ease-in-out infinite;
        }
        @keyframes goldShimmer {
            0%,
            100% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
        }
        @keyframes glowPulse {
            0%,
            100% {
                transform: scale(1);
                opacity: 0.5;
            }
            50% {
                transform: scale(1.1);
                opacity: 1;
            }
        }

        /* === NAME SECTION === */
        .name-section {
            text-align: center;
            animation: fadeInUp 1s ease-out 0.3s both;
            width: 100%;
            padding: 0 clamp(10px, 2vw, 20px);
        }
        .title-prefix {
            font-family: 'Amiri', serif;
            font-size: clamp(18px, 3vw, 22px);
            color: #b48c50;
            letter-spacing: clamp(2px, 0.6vw, 4px);
            margin-bottom: clamp(4px, 1vh, 8px);
            display: block;
            font-weight: 700;
        }
        .name {
            font-family: 'Cairo', sans-serif;
            font-weight: 900;
            font-size: clamp(28px, 6vw, 48px);
            line-height: 1.2;
            background: linear-gradient(135deg, #fff 0%, #b48c50 50%, #fff 100%);
            background-size: 200% 200%;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: shimmer 3s ease-in-out infinite;
            margin-bottom: clamp(10px, 2vh, 15px);
            word-break: break-word;
        }
        .divider {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: clamp(10px, 2vw, 15px);
            margin: clamp(10px, 2vh, 20px) 0;
        }
        .divider-line {
            width: clamp(30px, 8vw, 60px);
            height: 1px;
            background: linear-gradient(90deg, transparent, #b48c50);
        }
        .divider-line:last-child {
            background: linear-gradient(90deg, #b48c50, transparent);
        }
        .divider-diamond {
            width: clamp(8px, 1.2vw, 10px);
            height: clamp(8px, 1.2vw, 10px);
            background: #b48c50;
            transform: rotate(45deg);
            flex-shrink: 0;
        }
        .subtitle {
            font-family: 'Amiri', serif;
            font-size: clamp(14px, 2vw, 16px);
            color: rgba(255, 255, 255, 0.8);
            letter-spacing: clamp(1px, 0.3vw, 2px);
        }

        /* === ABOUT === */
        .about-section {
            text-align: center;
            max-width: min(700px, 92vw);
            width: 100%;
            padding: 0 clamp(10px, 2vw, 20px);
        }
        .section-title {
            font-family: 'Cairo', sans-serif;
            font-weight: 700;
            font-size: clamp(28px, 5vw, 36px);
            color: #b48c50;
            margin-bottom: clamp(20px, 4vh, 30px);
        }
        .section-text {
            font-family: 'Amiri', serif;
            font-size: clamp(16px, 2.4vw, 20px);
            line-height: 1.8;
            color: rgba(255, 255, 255, 0.85);
            margin-bottom: 20px;
        }
        .section-text-highlight {
            font-family: 'Amiri', serif;
            font-size: clamp(16px, 2.4vw, 20px);
            line-height: 1.8;
            color: #d4af37;
            font-weight: 700;
            padding: clamp(10px, 2vh, 15px) clamp(15px, 3vw, 25px);
            border-right: 3px solid #b48c50;
            border-left: 3px solid #b48c50;
            background: linear-gradient(90deg, rgba(180, 140, 80, 0.05), rgba(180, 140, 80, 0.1), rgba(180, 140, 80, 0.05));
            border-radius: 4px;
            margin-top: 10px;
            display: inline-block;
            max-width: 100%;
            word-break: break-word;
        }

        /* === CONTACT === */
        .contact-section {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: clamp(40px, 8vh, 80px) clamp(16px, 4vw, 40px);
            position: relative;
            text-align: center;
            width: 100%;
        }
        .contact-section .section-title {
            font-size: clamp(28px, 5vw, 38px);
            margin-bottom: 20px;
        }
        .contact-intro {
            font-family: 'Amiri', serif;
            font-size: clamp(16px, 2.4vw, 22px);
            line-height: 1.8;
            color: rgba(255, 255, 255, 0.85);
            max-width: min(700px, 92vw);
            margin-bottom: clamp(30px, 6vh, 50px);
            padding: 0 clamp(10px, 2vw, 20px);
        }
        .contact-intro span {
            color: #d4af37;
            font-weight: 700;
        }

        .social-buttons {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            align-items: center;
            gap: clamp(20px, 4vw, 30px);
            max-width: min(800px, 94vw);
            width: 100%;
            padding: 0 clamp(10px, 2vw, 20px);
        }
        .social-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: clamp(65px, 12vw, 90px);
            height: clamp(65px, 12vw, 90px);
            border-radius: 50%;
            overflow: hidden;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            text-decoration: none;
            position: relative;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
            border: 3px solid rgba(180, 140, 80, 0.3);
            background: #fff;
            flex-shrink: 0;
        }
        .social-btn img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
            display: block;
        }
        .social-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.2);
            transition: background 0.3s ease;
            z-index: 1;
        }
        .social-btn:hover::before {
            background: rgba(0, 0, 0, 0.05);
        }
        .social-btn:hover {
            transform: translateY(-8px) scale(1.05);
            border-color: #d4af37;
            box-shadow: 0 15px 50px rgba(180, 140, 80, 0.4);
        }
        .social-btn:active {
            transform: scale(0.92);
        }
        .social-btn:hover img {
            transform: scale(1.1);
        }
        .social-btn.instagram {
            border-color: #e1306c;
            box-shadow: 0 4px 25px rgba(225, 48, 108, 0.3);
        }
        .social-btn.instagram:hover {
            border-color: #e1306c;
            box-shadow: 0 8px 50px rgba(225, 48, 108, 0.5);
        }
        .social-btn.telegram {
            border-color: #0088cc;
            box-shadow: 0 4px 25px rgba(0, 136, 204, 0.3);
        }
        .social-btn.telegram:hover {
            border-color: #0088cc;
            box-shadow: 0 8px 50px rgba(0, 136, 204, 0.5);
        }
        .social-btn .tooltip {
            position: absolute;
            bottom: -40px;
            left: 50%;
            transform: translateX(-50%);
            font-family: 'Cairo', sans-serif;
            font-size: clamp(10px, 1.2vw, 12px);
            color: #fff;
            background: rgba(0, 0, 0, 0.85);
            padding: 4px 14px;
            border-radius: 20px;
            white-space: nowrap;
            opacity: 0;
            transition: opacity 0.3s ease, bottom 0.3s ease;
            pointer-events: none;
            z-index: 10;
            border: 1px solid rgba(180, 140, 80, 0.2);
        }
        .social-btn:hover .tooltip {
            opacity: 1;
            bottom: -35px;
        }

        .contact-divider {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: clamp(15px, 3vw, 20px);
            margin: clamp(20px, 4vh, 30px) auto clamp(30px, 6vh, 40px);
            width: min(80%, 500px);
            max-width: 500px;
        }
        .contact-divider .divider-line {
            flex: 1;
            height: 1px;
            background: linear-gradient(90deg, transparent, #b48c50);
        }
        .contact-divider .divider-line:last-child {
            background: linear-gradient(90deg, #b48c50, transparent);
        }
        .contact-divider .divider-diamond {
            width: clamp(10px, 1.5vw, 12px);
            height: clamp(10px, 1.5vw, 12px);
            background: #b48c50;
            transform: rotate(45deg);
            flex-shrink: 0;
        }
        .contact-footer {
            margin-top: clamp(40px, 8vh, 60px);
            font-family: 'Playfair Display', serif;
            font-size: clamp(11px, 1.4vw, 13px);
            letter-spacing: clamp(3px, 0.6vw, 5px);
            color: rgba(180, 140, 80, 0.5);
            text-transform: uppercase;
        }

        .footer {
            position: absolute;
            bottom: clamp(15px, 3vh, 30px);
            width: 100%;
            text-align: center;
            padding: 0 20px;
            pointer-events: none;
        }
        .footer-text {
            font-family: 'Playfair Display', serif;
            font-size: clamp(10px, 1.2vw, 12px);
            letter-spacing: clamp(3px, 0.6vw, 6px);
            color: rgba(180, 140, 80, 0.6);
            text-transform: uppercase;
        }

        /* === ANIMATIONS === */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        @keyframes shimmer {
            0%,
            100% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
        }

        /* === RESPONSIVE TWEAKS === */
        @media (max-width: 768px) {
            .image-wrapper {
                width: min(260px, 60vw, 60vh);
                height: min(260px, 60vw, 60vh);
            }
            .social-btn {
                width: clamp(60px, 14vw, 80px);
                height: clamp(60px, 14vw, 80px);
            }
            .social-buttons {
                gap: clamp(18px, 3vw, 25px);
            }
            .mode-indicator {
                top: 15px;
                right: 15px;
            }
        }

        @media (max-width: 480px) {
            .image-wrapper {
                width: min(210px, 55vw, 55vh);
                height: min(210px, 55vw, 55vh);
                padding: 4px;
            }
            .social-btn {
                width: clamp(55px, 16vw, 70px);
                height: clamp(55px, 16vw, 70px);
            }
            .social-buttons {
                gap: 15px;
            }
            .social-btn .tooltip {
                font-size: 10px;
                padding: 3px 10px;
            }
            .section {
                padding: 30px 12px;
            }
            .header-text {
                font-size: 10px;
                letter-spacing: 3px;
            }
            .footer-text {
                font-size: 9px;
                letter-spacing: 3px;
            }
        }

        @media (min-width: 1600px) {
            .image-wrapper {
                width: 320px;
                height: 320px;
            }
            .social-btn {
                width: 90px;
                height: 90px;
            }
            .name {
                font-size: 48px;
            }
            .section-title {
                font-size: 38px;
            }
        }
    </style>
</head>
<body>

    <canvas id="three-canvas"></canvas>
    <div class="bg-overlay"></div>

    <div class="loading-screen" id="loadingScreen">
        <div class="loading-ring"></div>
        <div class="loading-text">جاري التحميل...</div>
    </div>

    <div class="mode-indicator" id="modeIndicator">
        <div class="mode-dot" id="modeDot"></div>
        <span id="modeText">3D نشط</span>
    </div>

    <div class="scroll-progress" id="scrollProgress"></div>

    <div class="scroll-indicator" id="scrollIndicator">
        <span class="scroll-text">اسحب للأسفل</span>
        <div class="scroll-arrow"></div>
    </div>

    <section class="section hero">
        <div class="header">
            <div class="header-line">
                <span class="header-text">Photography</span>
            </div>
        </div>

        <div class="main-content">
            <div class="image-wrapper">
                <div class="image-inner">
                    <img src="data:image/jpeg;base64," alt="إ.أ. محمد علاء جبار سموم" />
                </div>
            </div>

            <div class="name-section">
                <span class="title-prefix">إ.أ.</span>
                <h1 class="name">محمد علاء جبار سموم</h1>
                <div class="divider">
                    <div class="divider-line"></div>
                    <div class="divider-diamond"></div>
                    <div class="divider-line"></div>
                </div>
                <p class="subtitle">Photographer &amp; Visual Artist</p>
            </div>
        </div>

        <div class="footer">
            <p class="footer-text">© 2026 — All Rights Reserved</p>
        </div>
    </section>

    <section class="section">
        <div class="about-section">
            <h2 class="section-title">نبذة عني</h2>
            <p class="section-text">
                مصور محترف بخبرة واسعة في مجال التصوير الفوتوغرافي، أسعى دائماً لالتقاط اللحظات المميزة وتحويلها إلى أعمال فنية خالدة.
            </p>
            <p class="section-text-highlight">
                ويعمل لدى مدير مكتب رئاسة لجنة الأمن والدفاع
            </p>
        </div>
    </section>

    <section class="contact-section" id="contact">
        <h2 class="section-title">تواصل معي</h2>

        <div class="contact-divider">
            <div class="divider-line"></div>
            <div class="divider-diamond"></div>
            <div class="divider-line"></div>
        </div>

        <p class="contact-intro">
            <span>أتشرف بالعمل معكم</span> —
            أنا هنا لتوثيق أجمل لحظاتكم وتحويل أفكاركم إلى صور تنبض بالحياة.
            <br />
            تواصلوا معي عبر حساباتي الاجتماعية:
        </p>

        <div class="social-buttons">
            <a href="https://www.instagram.com/_1vpn_?igsh=N3plamc4NTcyZ2pl" target="_blank" rel="noopener noreferrer" class="social-btn instagram" aria-label="Instagram">
                <img src="https://image.qwenlm.ai/public_source/06aaa638-0936-44ce-8840-c814521d5f0e/1b84f7723-8d05-4981-9ec6-63cd763a52af.png" alt="Instagram" />
                <span class="tooltip">@_1vpn_</span>
            </a>

            <a href="https://t.me/bdb_200" target="_blank" rel="noopener noreferrer" class="social-btn telegram" aria-label="Telegram">
                <img src="https://image.qwenlm.ai/public_source/06aaa638-0936-44ce-8840-c814521d5f0e/1e99a4d5a-e7df-4c7f-a9d8-2ed479b03eed.png" alt="Telegram" />
                <span class="tooltip">@bdb_200</span>
            </a>
        </div>

        <div class="contact-divider" style="margin-top: 50px;">
            <div class="divider-line"></div>
            <div class="divider-diamond"></div>
            <div class="divider-line"></div>
        </div>

        <p class="contact-footer">© 2026 — جميع الحقوق محفوظة</p>
    </section>

    <script>
        const canvas = document.getElementById('three-canvas');
        const scene = new THREE.Scene();
        const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
        const renderer = new THREE.WebGLRenderer({ canvas: canvas, antialias: true, alpha: true });

        renderer.setSize(window.innerWidth, window.innerHeight);
        renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
        renderer.setClearColor(0x0a0a0a, 1);
        camera.position.z = 30;

        let mouseX = 0,
            mouseY = 0,
            targetMouseX = 0,
            targetMouseY = 0;

        const particleCount = 2000;
        const particleGeometry = new THREE.BufferGeometry();
        const positions = new Float32Array(particleCount * 3);
        const colors = new Float32Array(particleCount * 3);
        const sizes = new Float32Array(particleCount);

        const goldColor = new THREE.Color(0xb48c50);
        const whiteColor = new THREE.Color(0xffffff);
        const darkGoldColor = new THREE.Color(0x8b6914);

        for (let i = 0; i < particleCount; i++) {
            positions[i * 3] = (Math.random() - 0.5) * 100;
            positions[i * 3 + 1] = (Math.random() - 0.5) * 100;
            positions[i * 3 + 2] = (Math.random() - 0.5) * 100;
            const choice = Math.random();
            let c = choice < 0.4 ? goldColor : choice < 0.7 ? whiteColor : darkGoldColor;
            colors[i * 3] = c.r;
            colors[i * 3 + 1] = c.g;
            colors[i * 3 + 2] = c.b;
            sizes[i] = Math.random() * 2 + 0.5;
        }

        particleGeometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
        particleGeometry.setAttribute('color', new THREE.BufferAttribute(colors, 3));
        particleGeometry.setAttribute('size', new THREE.BufferAttribute(sizes, 1));

        const particleMaterial = new THREE.PointsMaterial({ size: 0.15, vertexColors: true, transparent: true, opacity: 0.8,
            blending: THREE.AdditiveBlending, sizeAttenuation: true });
        const particles = new THREE.Points(particleGeometry, particleMaterial);
        scene.add(particles);

        // --- حُذفت جميع الأشكال الهندسية (torus, icosahedron, octahedron, torus knot, ring) ---

        // إضاءة خافتة للحفاظ على الأجواء
        scene.add(new THREE.AmbientLight(0x404040, 0.5));
        const light1 = new THREE.PointLight(0xb48c50, 2, 100);
        light1.position.set(20, 20, 20);
        scene.add(light1);
        const light2 = new THREE.PointLight(0xd4af37, 1.5, 100);
        light2.position.set(-20, -20, 10);
        scene.add(light2);
        const light3 = new THREE.PointLight(0xffffff, 1, 100);
        light3.position.set(0, 30, -10);
        scene.add(light3);

        // خطوط خلفية متحركة (اختياري، يمكن إزالتها أيضاً إذا أردت)
        const linesGroup = new THREE.Group();
        const lineMat = new THREE.LineBasicMaterial({ color: 0xb48c50, transparent: true, opacity: 0.1 });
        for (let i = 0; i < 20; i++) {
            const pts = [];
            const sx = (Math.random() - 0.5) * 80,
                sy = (Math.random() - 0.5) * 80,
                sz = (Math.random() - 0.5) * 60 - 20;
            pts.push(new THREE.Vector3(sx, sy, sz));
            pts.push(new THREE.Vector3(sx + (Math.random() - 0.5) * 20, sy + (Math.random() - 0.5) * 20, sz + (Math.random() -
                0.5) * 10));
            linesGroup.add(new THREE.Line(new THREE.BufferGeometry().setFromPoints(pts), lineMat));
        }
        scene.add(linesGroup);

        let time = 0;

        function animate() {
            requestAnimationFrame(animate);
            time += 0.005;
            mouseX += (targetMouseX - mouseX) * 0.05;
            mouseY += (targetMouseY - mouseY) * 0.05;

            particles.rotation.y = time * 0.3 + mouseX * 0.5;
            particles.rotation.x = time * 0.1 + mouseY * 0.3;

            const pos = particleGeometry.attributes.position.array;
            for (let i = 0; i < particleCount; i++) pos[i * 3 + 1] += Math.sin(time * 2 + i * 0.01) * 0.02;
            particleGeometry.attributes.position.needsUpdate = true;

            // تدوير الخطوط فقط (بدون أشكال)
            linesGroup.rotation.y = time * 0.1;
            linesGroup.rotation.x = time * 0.05;

            camera.position.x = mouseX * 2;
            camera.position.y = -mouseY * 2;
            camera.lookAt(scene.position);
            light1.intensity = 2 + Math.sin(time * 3) * 0.5;
            light2.intensity = 1.5 + Math.cos(time * 2) * 0.3;
            renderer.render(scene, camera);
        }

        document.addEventListener('mousemove', (e) => {
            targetMouseX = (e.clientX / window.innerWidth - 0.5) * 2;
            targetMouseY = (e.clientY / window.innerHeight - 0.5) * 2;
        });
        document.addEventListener('touchmove', (e) => {
            if (e.touches.length) {
                targetMouseX = (e.touches[0].clientX / window.innerWidth - 0.5) * 2;
                targetMouseY = (e.touches[0].clientY / window.innerHeight - 0.5) * 2;
            }
        }, { passive: true });
        window.addEventListener('resize', () => {
            camera.aspect = window.innerWidth / window.innerHeight;
            camera.updateProjectionMatrix();
            renderer.setSize(window.innerWidth, window.innerHeight);
        });
        window.addEventListener('scroll', () => {
            const s = window.scrollY * 0.001;
            camera.position.z = 30 - s * 5;
            const scrollY = window.scrollY;
            const docH = document.documentElement.scrollHeight - window.innerHeight;
            const pct = docH > 0 ? (scrollY / docH) * 100 : 0;
            document.getElementById('scrollProgress').style.width = pct + '%';
            const indicator = document.getElementById('scrollIndicator');
            if (scrollY > 100) indicator.classList.add('hidden');
            else indicator.classList.remove('hidden');
        }, { passive: true });

        window.addEventListener('load', () => {
            setTimeout(() => {
                document.getElementById('loadingScreen').classList.add('hidden');
                document.getElementById('modeIndicator').classList.add('visible');
                document.getElementById('modeDot').classList.add('active');
            }, 1500);
        });

        animate();

        const obs = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, { threshold: 0.1, rootMargin: '0px 0px -100px 0px' });

        document.querySelectorAll('.about-section, .contact-section').forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(30px)';
            el.style.transition = 'opacity 0.8s ease, transform 0.8s ease';
            obs.observe(el);
        });
    </script>
</body>
</html>